- hosts: servers
  become: true
  tasks:
    - name: Update Packages
      apt:
        update_cache: yes

    - name: Install Docker
      apt:
        name: docker.io
        state: present

    - name: Enable Docker
      service:
        name: docker
        enabled: yes
        state: started